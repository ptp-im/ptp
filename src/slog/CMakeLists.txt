INCLUDE_DIRECTORIES(./)
INCLUDE_DIRECTORIES(../../include)
set(LIB_NAME_MAIN slog)

LINK_DIRECTORIES(${LIBRARY_OUTPUT_PATH})

add_library(${LIB_NAME_MAIN} STATIC slog_api.cpp)
TARGET_LINK_LIBRARIES(${LIB_NAME_MAIN} log4cxx)
